# Source PDFs

Official AFPC PDF URLs are tracked in `../README.md` and `../af-pfra-2026.json`.

AFPC blocked direct command-line PDF downloads in this environment with an access-denied response, so PDF binaries are not checked in yet. Once a clean PDF is obtained manually or through an allowed browser download, place it here and record its filename/checksum in the standards manifest.
